"""Test package for SKY130 Flow GUI.

Declaring the package lets ``python -m unittest discover -s tests -t .`` put the
repository root on ``sys.path``, so the ``app`` package is importable from every
test module.
"""

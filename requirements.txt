# PySide6-Essentials instead of the full PySide6: the addons wheel bundles
# Qt WebEngine (a ~195 MB Chromium) plus Quick3D and Designer, none of which
# this app imports. Anyone who already has the full PySide6 is unaffected.
PySide6-Essentials>=6.7
pyqtgraph>=0.13
numpy>=1.24
